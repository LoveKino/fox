import React, { Component } from 'react';

export default class Modal extends Component {

    render () {
        return (
            <fox-panel-container class="modal">
            </fox-panel-container>
        );
    }
}
