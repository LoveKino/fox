import React, { Component } from 'react';

export default class RightPanel extends Component {

    render () {
        return (
            <fox-panel-container className="right">
            </fox-panel-container>
        );
    }
}
